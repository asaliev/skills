interface:
  display_name: "Code Review"
  short_description: "Code review with prioritized findings"
  default_prompt: "Use $code-review to review the current changes and report the highest-risk findings first."

policy:
  allow_implicit_invocation: true

